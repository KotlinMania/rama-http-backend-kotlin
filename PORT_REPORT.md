# Port report — `rama-http-backend-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `rama-http-backend`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 9 `use` sites from 1 other repos
- **Outbound**: 131 `use` sites to 6 other repos
- **Unresolved external**: 5 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `rama-http-backend` | `rama_http_backend` | 9 | 6 | COD-108 | 73 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (9 edges, 1 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-network-proxy` | 9 | ✅ |

### Symbol-level demand (7 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `rama_http_backend::server::HttpServer` | 2 | 2 | 0 | 1 |
| `rama_http_backend::server::layer::upgrade::Upgraded` | 2 | 2 | 0 | 1 |
| `rama_http_backend::client::HttpClientService` | 1 | 1 | 0 | 1 |
| `rama_http_backend::client::HttpConnector` | 1 | 1 | 0 | 1 |
| `rama_http_backend::client::proxy::layer::HttpProxyConnector` | 1 | 1 | 0 | 1 |
| `rama_http_backend::client::proxy::layer::HttpProxyConnectorLayer` | 1 | 1 | 0 | 1 |
| `rama_http_backend::server::layer::upgrade::UpgradeLayer` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 7 symbols)

##### `rama_http_backend::server::HttpServer` (2 sites, 2 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/http_proxy.rs:57` — `use rama_http_backend::server::HttpServer`
- `codex-kotlin/codex-network-proxy` `src/mitm.rs:34` — `use rama_http_backend::server::HttpServer`

##### `rama_http_backend::server::layer::upgrade::Upgraded` (2 sites, 2 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/http_proxy.rs:59` — `use rama_http_backend::server::layer::upgrade::Upgraded`
- `codex-kotlin/codex-network-proxy` `src/mitm.rs:35` — `use rama_http_backend::server::layer::upgrade::Upgraded`

##### `rama_http_backend::client::HttpClientService` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/upstream.rs:15` — `use rama_http_backend::client::HttpClientService`

##### `rama_http_backend::client::HttpConnector` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/upstream.rs:16` — `use rama_http_backend::client::HttpConnector`

##### `rama_http_backend::client::proxy::layer::HttpProxyConnector` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/http_proxy.rs:56` — `use rama_http_backend::client::proxy::layer::HttpProxyConnector`

##### `rama_http_backend::client::proxy::layer::HttpProxyConnectorLayer` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/upstream.rs:17` — `use rama_http_backend::client::proxy::layer::HttpProxyConnectorLayer`

##### `rama_http_backend::server::layer::upgrade::UpgradeLayer` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/http_proxy.rs:58` — `use rama_http_backend::server::layer::upgrade::UpgradeLayer`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `tokio-kotlin` | 6 | 5 | `tokio` | COD-156 | 10 | To Do |
| `rama-core-kotlin` | 75 | 20 | `rama-core` | COD-106 | 48 | To Do |
| `rama-net-kotlin` | 21 | 12 | `rama-net` | COD-109 | 52 | To Do |
| `rama-tcp-kotlin` | 1 | 1 | `rama-tcp` | COD-111 | 59 | To Do |
| `rama-http-kotlin` | 27 | 18 | `rama-http` | COD-107 | 64 | To Do |
| `rama-unix-kotlin` | 1 | 1 | `rama-unix` | COD-113 | 65 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `rama-http-types` | 253 | 106 | `rama-http-backend` |
| `rama-utils` | 196 | 36 | `rama-http-backend` |
| `pin-project-lite` | 118 | 1 | `rama-http-backend` |
| `rama-http-headers` | 75 | 38 | `rama-http-backend` |
| `rama-http-core` | 13 | 10 | `rama-http-backend` |
