# Port targets — `rama-http-backend-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `rama-http-backend` that codex-rs uses, and therefore the minimum subset that `rama-http-backend-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-108] (rank `73`, tier `direct`, status `To Do`)

**Cargo package name**: `rama-http-backend`
**Rust module name (in `use` statements)**: `rama_http_backend`

## Summary

- **7** distinct symbols imported by codex-rs
- **9** total `use` statements
- **3** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `rama_http_backend::server::HttpServer`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/http_proxy.rs:57` (`codex-network-proxy`) — `use rama_http_backend::server::HttpServer`
- `network-proxy/src/mitm.rs:34` (`codex-network-proxy`) — `use rama_http_backend::server::HttpServer`

### `rama_http_backend::server::layer::upgrade::Upgraded`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/http_proxy.rs:59` (`codex-network-proxy`) — `use rama_http_backend::server::layer::upgrade::Upgraded`
- `network-proxy/src/mitm.rs:35` (`codex-network-proxy`) — `use rama_http_backend::server::layer::upgrade::Upgraded`

### `rama_http_backend::client::proxy::layer::HttpProxyConnector`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/http_proxy.rs:56` (`codex-network-proxy`) — `use rama_http_backend::client::proxy::layer::HttpProxyConnector`

### `rama_http_backend::server::layer::upgrade::UpgradeLayer`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/http_proxy.rs:58` (`codex-network-proxy`) — `use rama_http_backend::server::layer::upgrade::UpgradeLayer`

### `rama_http_backend::client::HttpClientService`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/upstream.rs:15` (`codex-network-proxy`) — `use rama_http_backend::client::HttpClientService`

### `rama_http_backend::client::HttpConnector`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/upstream.rs:16` (`codex-network-proxy`) — `use rama_http_backend::client::HttpConnector`

### `rama_http_backend::client::proxy::layer::HttpProxyConnectorLayer`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/upstream.rs:17` (`codex-network-proxy`) — `use rama_http_backend::client::proxy::layer::HttpProxyConnectorLayer`
