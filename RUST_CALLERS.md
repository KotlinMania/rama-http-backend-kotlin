# Rust callers — `rama-http-backend-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `rama-http-backend` | `rama_http_backend` | 9 | 1 | 1 | 9 |

## `rama-http-backend`

### Inbound symbols (7 distinct, 9 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `rama-http-backend`.

#### `rama_http_backend::server::HttpServer` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/http_proxy.rs:57` — `use rama_http_backend::server::HttpServer`
- ✅ `codex-kotlin/codex-network-proxy` `src/mitm.rs:34` — `use rama_http_backend::server::HttpServer`

#### `rama_http_backend::server::layer::upgrade::Upgraded` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/http_proxy.rs:59` — `use rama_http_backend::server::layer::upgrade::Upgraded`
- ✅ `codex-kotlin/codex-network-proxy` `src/mitm.rs:35` — `use rama_http_backend::server::layer::upgrade::Upgraded`

#### `rama_http_backend::client::HttpClientService` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/upstream.rs:15` — `use rama_http_backend::client::HttpClientService`

#### `rama_http_backend::client::HttpConnector` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/upstream.rs:16` — `use rama_http_backend::client::HttpConnector`

#### `rama_http_backend::client::proxy::layer::HttpProxyConnector` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/http_proxy.rs:56` — `use rama_http_backend::client::proxy::layer::HttpProxyConnector`

#### `rama_http_backend::client::proxy::layer::HttpProxyConnectorLayer` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/upstream.rs:17` — `use rama_http_backend::client::proxy::layer::HttpProxyConnectorLayer`

#### `rama_http_backend::server::layer::upgrade::UpgradeLayer` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/http_proxy.rs:58` — `use rama_http_backend::server::layer::upgrade::UpgradeLayer`
